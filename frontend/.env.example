# Local development: leave unset to use the Vite dev proxy at /api → localhost:3000
# PUBLIC_API_URL=http://localhost:3000

# Production (Vercel): set to your Render backend URL
# PUBLIC_API_URL=https://your-backend.onrender.com
